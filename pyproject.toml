# This repository contains macOS scripts rather than a distributable Python
# package. Tool configuration lives in ruff.toml, pytest.ini, and
# pyrightconfig.json, so no project metadata or dependencies are needed here.
